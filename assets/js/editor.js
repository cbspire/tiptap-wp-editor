/* TipTap Editor — compiled bundle placeholder.
 * Run `npm run build` to compile the actual bundle from src/.
 * This file is committed so the plugin is installable without a build step,
 * but it will be overwritten by the build process.
 */
( function() {
	'use strict';
	/* placeholder — run npm run build */
} )();
